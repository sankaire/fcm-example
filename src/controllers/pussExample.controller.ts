import {sendPushNotification, sendTransactionPushNotification} from "../utils/services/pushNotification";
import {Request,Response} from "express";

export const topicPushExample = async (req:Request, res:Response) =>{
    //do something
    //
    // sendPushNotification(params)
    return
}
export const tokenPushExample = async (req:Request, res:Response) =>{
    //do something
    //
    // sendTransactionPushNotification(params)
    return
}